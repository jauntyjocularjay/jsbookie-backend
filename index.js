
import {api, request, sport} from './the-odds-api.js';

// api.debug = false;


// request.odds(sport.nhl);

// request.dbkey;
// dbfn.list();
// let dbkey = sport.nhl.dbkey;
// dbfn.logRow(dbkey);
// dbfn.logOdds(dbkey);

request.odds(sport.nhl);

// request.odds(sport.nhl);
